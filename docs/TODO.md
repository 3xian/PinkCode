# PinkCode TODO

## ACP capabilities

- [x] SessionMode `ask` — read-only Q&A mode via `session/set_mode({ modeId: "ask" })`
- [x] `x.ai/session/usage` — live turn tokens and cost
- [x] `x.ai/recap` / `x.ai/rewind/*` / compact — history management
- [x] `x.ai/subagent/cancel` and subagent list
- [x] `x.ai/task/list` / `x.ai/task/kill` — background-task control
- [x] `x.ai/session_notification` — pending interaction, subagent, and interaction-resolved events
- [x] `set_session_model` — change model during a session

## Git

- [x] Real-time status — branch, upstream, ahead/behind, staged, and unstaged
- [x] Inline file diff viewer
- [x] Interactive hunk selection for commits
