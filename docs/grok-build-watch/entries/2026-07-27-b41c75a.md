# 2026-07-27 — monorepo sync `b41c75a`

| | |
|--|--|
| Noted | 2026-07-27 |
| Remote | `b41c75a578f98bddbd326ab02cd53618451d97ee` (2026-07-26 20:03 +0100) |
| Prev HEAD | `47348d13ec4508dcfe440e34c6d511bb02998fb2` (2026-07-25) |
| xai-grok-shell | `0.2.112` (not bumped) |
| Size | 1 commit · **92 files** · **+9410 / -3788** |
| PinkCode | **done · 2026-07-30 · plan copy + dynamic models + ACP timeouts** |

### Upstream changelog

1. Detect the herdr multiplexer
2. Mark `/gboom` as non-production code
3. Bound peak memory when loading a large session
4. Add a subagent lifecycle soak bounding threads, fds, and heap
5. Stream inherited replay to bound fork memory
6. Copy full plan from plan approval with `y`
7. Stop armed signature verification from deleting the managed-deny smoke policy
8. Add source-tagged terminal version telemetry
9. Show the UI instantly and fetch models and settings in the background
10. Session test helpers
11. `computer_reason` on the `ConversationHistoryDone` trailer

### Notes

**Non-blocking startup (important)**  
Leader/agent no longer block on `/v1/models` + settings; catalog uses `spawn_background_refresh`. HTTP startup timeouts: `STARTUP_FETCH_TIMEOUT` 5s, `STARTUP_AUTH_TIMEOUT` 60s, `MIN_CLIENT_CONNECT_TIMEOUT` 120s, etc. `shared_blocking_client` → `shared_startup_blocking_client`.

**Large session / fork**  
Peak memory bounds; fork replay is streaming. Rewind filters shared via `filter_rewind_by` + `RewindStep`.

**Other (mostly agent/TUI)**  
Models module split; OTEL fail-closed gate; plan approval `y` = copy full plan; herdr mux; `term_version` source tags; `/gboom` non-prod; signature/smoke fix; subagent soak tests; `computer_reason` on history trailer.

### PinkCode impact

| Area | Notes | Status |
|------|-------|--------|
| Models at ready | Catalog may be empty briefly; need `availableModels` + later refresh | **done** |
| HTTP client rename | Only if linking `xai-grok-http` | **n/a** |
| Session load | Agent-side memory/stream; raise load timeout if needed | **n/a** (load timeout 120s) |
| Plan approval UX | TUI copies plan with `y` | **done** |
| Timeouts | Align auth/connect with startup constants | **done** |

### Digested

- **When:** 2026-07-30
- **What:**
  1. Plan approval: Copy plan + `y`/`a`/`s`/`q`
  2. Parse `availableModels`; handle `x.ai/models/update`; dynamic model dropdown with fallback
  3. ACP: authenticate 60s; `session/load` 120s
- **Paths:** `PlanApprovalModal.tsx`, `PromptBar.tsx`, `acp/protocol.rs`, `acp/mod.rs`, `agent_manager.rs`, `agent_types.rs`, `liveTimeline.ts`
